# Binary Search

list = list(1, 2, 3, 7 ,5, 9)

input = as.numeric(readline("Enter the num. to search: "))

# search
bsearch <- function(input){
  len = length(list)
  mid = floor(temp/2)
  midnum = 
  if(as.numeric() == input)
}

#######
lsearch(input)